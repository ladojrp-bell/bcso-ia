<nav class="bg-white shadow p-4 mb-6">
  <div class="container mx-auto flex justify-between items-center">
    <div class="flex space-x-4">
      <a href="/" class="text-xl font-bold">IA Dashboard</a>
      <% if (session.role === 'admin') { %>
        <a href="/admin/users" class="text-blue-600 hover:underline">Manage Users</a>
      <% } %>
    </div>
    <% if (session.user) { %>
      <div>
        <span class="text-sm">Signed in as <%= session.user %></span>
        <a href="/logout" class="text-red-500 hover:underline ml-4">Logout</a>
      </div>
    <% } %>
  </div>
</nav>